{{#if description.length}}
/**
 * {{{ description }}}
 */
 {{/if}}
export interface {{{jsIdentifier name}}} {
	{{#each properties}}
  {{{name}}}{{~#unless isRequired}}?{{/unless}}: {{{jsIdentifier type}}};
  {{/each}}
}
{{#if nestedModels.length}}
{{#each nestedModels}}
{{> modelClassTemplate name=(concat "_" ../name name)}}

{{/each}}
{{/if}}
